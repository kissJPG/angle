name: Build ANGLE for Android ARM64 (Docker)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:22.04
      options: --user root    # 容器内以 root 运行，无需 sudo
    timeout-minutes: 180

    env:
      DEPOT_TOOLS_PATH: /__w/depot_tools    # 容器内默认挂载工作空间为 /__w
      ANGLE_SRC: /__w/angle

    steps:
      - name: Install system dependencies
        run: |
          apt-get update
          apt-get install -y \
            curl git python3 python3-distutils python3-setuptools \
            default-jdk \
            ninja-build \
            lsb-release
          # 保证 python 指向 python3
          ln -s /usr/bin/python3 /usr/bin/python

      - name: Cache depot_tools
        id: cache-depot-tools
        uses: actions/cache@v4
        with:
          path: ${{ env.DEPOT_TOOLS_PATH }}
          key: depot_tools-${{ runner.os }}-docker-v1

      - name: Get depot_tools
        if: steps.cache-depot-tools.outputs.cache-hit != 'true'
        run: |
          git clone --depth 1 https://chromium.googlesource.com/chromium/tools/depot_tools.git \
            ${{ env.DEPOT_TOOLS_PATH }}

      - name: Add depot_tools to PATH
        run: echo "${{ env.DEPOT_TOOLS_PATH }}" >> $GITHUB_PATH

      - name: Configure gclient for Android
        run: |
          cat <<EOF > .gclient
          solutions = [
            {
              "name"        : "angle",
              "url"         : "https://chromium.googlesource.com/angle/angle.git",
              "deps_file"   : "DEPS",
              "managed"     : False,
              "custom_deps" : {},
            },
          ]
          target_os = ["android"]
          EOF

      - name: Sync ANGLE source and Android dependencies
        run: |
          gclient sync --no-history --shallow --with_branch_heads

      - name: Configure GN for Android ARM64
        run: |
          cd angle
          gn gen out/Android-arm64 --args='
            target_os = "android"
            target_cpu = "arm64"
            is_debug = false
            is_component_build = false
            angle_enable_vulkan = true
            angle_enable_gl = true
            angle_enable_egl = true
            angle_build_all = true
          '

      - name: Build ANGLE
        run: |
          cd angle
          autoninja -C out/Android-arm64

      - name: Upload built libraries
        uses: actions/upload-artifact@v4
        with:
          name: angle-android-arm64
          path: |
            angle/out/Android-arm64/lib*.so
            angle/out/Android-arm64/*.a